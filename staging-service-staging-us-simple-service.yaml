apiVersion: v1
kind: Service
metadata:
  name: staging-us-simple-service
  namespace: staging
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: trivial-go-web-app
  type: ClusterIP
